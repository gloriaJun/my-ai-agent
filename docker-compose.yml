services:
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: ollama
  #   environment:
  #     TZ: Asia/Seoul
  #   volumes:
  #     - ./data/ollama:/root/.ollama
  #   ports:
  #     - "11434:11434"
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 16G
  #       reservations:
  #         memory: 4G
  #   logging:
  #     driver: "json-file"
  #     options:
  #       max-size: "50m"
  #       max-file: "3"
  #   restart: unless-stopped

  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    # ports:
    # - "5678:5678"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      TZ: Asia/Seoul
      N8N_LOG_LEVEL: info
      N8N_PATH: ${N8N_BASE_PATH:-/}
      OLLAMA_HOST: http://host.docker.internal:11434
      WEBHOOK_URL: ${WEBHOOK_URL}
    volumes:
      - ./data/n8n:/home/node/.n8n
      # - ~/Documents/GitHubPrivate:/workspace
      # - ~/Documents/GitHubPrivate/my-ai-agent:/workspace/my-ai-agent:ro
    deploy:
      resources:
        limits:
          memory: 4G
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"
    restart: unless-stopped
    # depends_on:
    #   - ollama

  openclaw:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: openclaw
    # ports:
    #   - "18789:18789"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    environment:
      TZ: Asia/Seoul
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      DEFAULT_MODEL: "qwen2.5:1.5b"
    volumes:
      - ./data/openclaw:/home/node/.openclaw
      - ./.env:/home/node/.openclaw/.env:ro
      - /var/tmp/openclaw-compile-cache:/var/tmp/openclaw-compile-cache
      # - ~/Documents/GitHubPrivate:/workspace
      # - ~/Documents/GitHubPrivate/my-ai-agent:/workspace/my-ai-agent:ro
    command: ["openclaw", "gateway", "run"]
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    # depends_on:
    #   - ollama

networks:
  default:
    external: true
    name: proxy-net
